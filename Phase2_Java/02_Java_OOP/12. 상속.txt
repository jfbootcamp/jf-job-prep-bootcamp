1. 상속 
    1) 객체 지향 프로그래밍의 핵심 요소 중 하나 
    2) 기존 클래스의 필드와 메서드를 새로운 클래스에서 재사용하게 해줌 
    3) 기존 클래스의 속성과 기능을 그대로 물려받는 것임 
    4) 상속 하려면 extends 키워드를 사용하면 됨
    5) extends 대상은 하나만 선택할 수 있음 

2. 상속 관계
    1) 용어 정리
        - 부모 클래스 (슈퍼 클래스)
            - 상속을 통해 자신의 필드와 메서드를 다른 클래스에 제공하는 클래스 
        - 자식 클래스 (서브 클래스)
            - 부모 클래스로부터 필드와 메서드를 상속받는 클래스 
    2) 단일 상속 (Single Inheritance)  
        - 자바는 다중 상속(Multiple Inheritance)을 지원하지 않음
        - 부모는 하나만 선택할 수 있음 
            - 부모가 또 다른 부모를 하나 가지는 것은 가능함.

3. 상속과 메모리 구조 
    1) DmbCellPhone  dmbCellPhone = new DmbCellPhone();  
        - DmbCellPhone 객체 뿐만 아니라 상속 관계에 있는 CellPhone 객체까지 함께 포함해서 
          인스턴스를 생성함 
        - 상속 관계를 사용하면 부모 클래스도 함께 포함해서 생성함.
    2) 상속 관계의 객체 생성
        - 그 내부에는 부모와 자식이 모두 생성됨    
    3) 상속 관계에서 기능 추가와 클래스 확장 

4. 상속과 메서드 오버라이딩
    1) 부모 타입의 기능을 자식에서는 다르게 재정의 할 수 있음 
    2) 부모에게서 상속 받은 기능을 자식이 재정의 하는 것
        - 메서드 오버라이딩 (Overriding)
        - @Override 
            - @이 붙은 부분을 애노테이션이라 함 
            - 특별한 주석 
                - 프로그램이 읽을 수 있는 특별한 주석 
            - 컴파일러는 이 애노테이션을 보고 메서드가 정확히 오버라이드 되었는지 확인함 
        - 상위 클래스의 메서드를 오버라이드(재정의)하는 것임      

5. 오버로딩(Overloading)과 오버라이딩(Overriding)
    1) 메서드 오버로딩 
        - 메서드 이름이 같고 매개변수(파라미터)가 다른 메서드 
        - 같은 이름의 메서드를 여러개 정의함 
    2) 메서드 오버라이딩 
        - 하위 클래스에서 상위 클래스의 메서드를 재정하는 것임 
        - 상속 관계에서 사용함 
        - 메서드 오버라이딩 조건 
            - 메서드 이름: 같아야 함
            - 메서드 매개변수(파라미터): 타입, 순서, 개수가 같아야 함
            - 반환 타입: 같아야 함
            - 접근 제한자 : 상위클래스보다 더 제한적이어서는 안 됨  
        - static, final, private 키워드가 붙은 메서드는 오버라이딩 될 수 없음 
        - 생성자 오버라이딩 할수 없음 

6. 상속과 접근 제어 
    1) 기호 
        + public        모든 외부 호출을 허용
        # protected     같은 패키지안에서 호출 허용
                        패키지가 달라도 상속 관계의 호출은 허용 
        ~ (default)     같은 패키지안에서 호출 허용
        - private       모든 외부 호출을 막음    

7. super
    1) 부모 클래스에 대한 참조를 나타냄 
        - 부모 클래스의 기능을 사용한 것을 확인할 수 있음.

8. super()
    1) 상속 관계의 인스턴스를 생성하면 메모리 내부에는 
       자식과 부모 클래스의 객체가 각각 다 만들어짐.
       - Child를 만들면 부모인 Parent까지 함께 만들어짐.
       - 각각의 생성자도 모두 호출되어야 함 
    2) 상속 관계를 사용하면 자식 클래스의 생성자에서 부모 클래스의 생성자를 반드시
       호출해야 함 (Rule)
    3) 상속 관계에서 객체 생성
        - 생성자의 실행 순서가 결과적으로 최상위 부모부터 실행되어서 하나씩 아래로 내려오는 것임
        - 초기화는 최상위 부모부터 이루어짐 
            - 자식 생성자의 첫줄에서 부모의 생성자를 호출해야 하기 때문임.
        - 상속 관계의 생성자 호출은 결과적으로 부모에서 자식 순서로 실행됨.
            - 부모의 데이터를 먼저 초기화하고 그 다음에 자식의 데이터를 초기화됨 
    4) 상속 관계에서 자식 클래스의 생성자 첫줄에 반드시 super(...)를 호출해야 함 
        - 단 기본 생성자인 경우 생략할수 있음 (super())            

